#include<stdio.h>
main()
 {
 	int a,b,x,y;
 	printf("enter a and b values");
 	scanf("%d%d", &a,&b);
    x=(a-b);
 	y=(a*b);
 	printf("the difference of two numbers is %d", x);
    printf("\nthe multiplication of two numbers is %d", y);
	 }
